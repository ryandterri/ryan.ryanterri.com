name: Build app

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: monster

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: npm install
        run: npm install

      - name: build
        run: npm run build
